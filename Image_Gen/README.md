# AI Image Generator 

Цей проєкт — веб-додаток для генерації зображень за допомогою штучного інтелекту. Реалізовано підтримку різних моделей (Stable Diffusion, SDXL), оптимізацію для роботи на CPU та зручний веб-інтерфейс.

## Реалізовані завдання

У проєкті виконано завдання всіх рівнів складності:

### Базовий рівень
- **Інтеграція SDXL:** Додано підтримку моделі `stable-diffusion-xl-base-1.0` з автоматичним перемиканням пайплайнів.
-  **Batch генерація:** Реалізовано метод для створення серії зображень.
-  **Система конфігурації:** Налаштування зберігаються та завантажуються з `config.json`.

### Середній рівень
-  **Image-to-Image:** Можливість генерації нових зображень на основі вхідної картинки (з автоматичним ресайзом та керуванням силою змін).
-  **Порівняння моделей:** Автоматична генерація HTML-звіту з порівнянням результатів різних моделей на одному сінді.
-  **Шаблони промптів:** Система підстановки змінних у заготовлені шаблони (наприклад, `{style}`, `{subject}`).

### Високий рівень
-  **Веб-інтерфейс:** Повноцінний сайт на **Flask** з темною темою ("Cyberpunk style").
-  **Адаптивний UI:** Використання Bootstrap 5, індикатор завантаження, вибір параметрів.

### Бонуси та Технічні рішення
-  **Docker контейнеризація:** Проєкт повністю загорнуто в Docker.
-  **CPU Optimization:** Автоматичне визначення пристрою (CPU/GPU). Для CPU використовується `float32` та зменшені розміри зображень для економії пам'яті.
-  **Error Handling:** Система не падає при помилках завантаження моделей.

---

## 🛠 Інструкція по встановленню

Ви можете запустити проєкт двома способами.

### Спосіб 1: Через Docker (Рекомендовано)
Найпростіший спосіб, не потребує налаштування Python.

1. **Створіть образ:**
   ```bash
   docker build -t ai-generator .

2. **Запустіть контейнер:**
   ```bash
   docker run -p 5000:5000 -v huggingface_cache:/root/.cache/huggingface ai-generator

3. **Відкрийте у браузері: Перейдіть за посиланням: http://localhost:5000**


### Спосіб 2: Локальний запуск (Python)
Якщо ви хочете запускати без Docker.

1. **Встановіть залежності:**
   ```bash
   pip install -r requirements.txt
   
2. **Запустіть сервер:**
      ```bash
   python app.py
   
3. **Відкрийте у браузері: http://127.0.0.1:5000**


## Приклади роботи

### 1. Веб-інтерфейс 
Інтуїтивно зрозумілий інтерфейс із налаштуваннями моделі, промпту та кроків генерації.

![img.png](img.png)

### 2. Приклад генерації 

![sd_v1.5_20251230_190220_0.png](outputs/sd_v1.5_20251230_190220_0.png)

### 3. HTML Звіт порівняння
Приклад автоматично згенерованого звіту порівняння моделей.

![img_1.png](img_1.png)



## 📂 Структура проєкту

* `app.py` — Головний файл веб-сервера.
* `generator.py` — Логіка ML, класи для роботи з Diffusers.
* `templates/index.html` — HTML шаблон інтерфейсу.
* `Dockerfile` — Інструкції для збірки Docker образу.
* `config.json` — Конфігурація генератора.
* `outputs/` — Папка зі збереженими результатами.
