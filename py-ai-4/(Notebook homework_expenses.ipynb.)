expenses = [1200, 1500, 800, 600, 1000, 400, 900]

total = sum(expenses)
average = total / len(expenses)
max_expense = max(expenses)
min_expense = min(expenses)
days_over_1000 = sum(1 for x in expenses if x > 1000)

total, average, max_expense, min_expense, days_over_1000

/////////////////////////////////////////////Markdown///////////////////////////////////

| Показник            | Значення |
|---------------------|----------|
| Загальна сума       | 6400     |
| Середнє значення    | 914.29   |
| Максимальна витрата | 1500     |
| Мінімальна витрата  | 400      |
| Днів > 1000         | 2        |
